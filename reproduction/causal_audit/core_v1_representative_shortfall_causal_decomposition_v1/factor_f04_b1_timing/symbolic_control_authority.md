# F04 symbolic authority

`p_next=p+dt*v`; `v_next=v+dt*u`; `p(tau)=p+tau*v`; therefore `∂p(tau)/∂u=0` for `tau∈[0,dt]` under the frozen position-first Euler model. S2/S3 are E2 diagnostics, never Core V1 replacements.
