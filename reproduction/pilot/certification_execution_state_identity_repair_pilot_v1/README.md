# Certification–Execution State Identity Repair Engineering Pilot V1

This directory freezes an engineering pilot protocol and a future execution harness. The freeze task does **not** create the pilot result root, run GPU code, launch tmux, execute a trial, or perform scientific analysis.

The sole upstream runtime is R6 commit `601204bfc14e3ad2c8e3c714b8f5045829491635`. The sole upstream smoke authority is the immutable retry5 root named in `PILOT_PROTOCOL.json`; its complete 30-file path/size/SHA256 inventory is `UPSTREAM_SMOKE_EVIDENCE_MANIFEST.json`. The harness reuses the R6 smoke runner's delegate projection, child authorization, observation tap, and executed-action-bound continuity audit without changing runtime source. Its own trial acceptance and aggregation enforce the pilot schema and non-500-specific denominator.

The frozen order is `[5,15,25,35,45,55,65,75,85,95]`, seed 0, at most 500 completed cycles per trial, serial and separate-process, no automatic retry. This is a reused, outcome-exposed engineering cohort, **not** a scientific holdout. Deadline WARNING/EXPIRED and L3 FAIL are recorded as engineering facts, not automatic pilot failures; only frozen integrity/identity/evidence gates decide pilot PASS.

The first freeze at `e859fbd48384cdcc39162f4beb26ed26c387057c` remains historically **BLOCKED**. Its `PILOT_EXECUTION_LOCK.json` is immutable and inactive. The repaired active pointer is `PILOT_EXECUTION_LOCK_REPAIR_R1.json`; it supersedes the blocked lock without changing `PILOT_PROTOCOL.json`. R6 smoke task files are engineering-harness lineage checked by exact Git tree and frozen byte hashes, while scientific/runtime paths retain a separate hard diff-zero gate.

Attempt0 at `/disk1/zlab/v3_repair_records/cert_exec_identity_repair_pilot_v1_20260916` failed before its first child process because the task-local parent copied integer `PILOT_PROTOCOL.json.environment.physical_gpu` into `subprocess.Popen(env=...)`. `PILOT_ATTEMPT0_IMMUTABLE_EVIDENCE_MANIFEST.json` preserves that failure without editing the result root. The R2 task-local child boundary explicitly converts permitted scalar environment values to strings and rejects unsupported values before `Popen`. The old execution locks remain byte-identical; `PILOT_EXECUTION_LOCK_LAUNCH_REPAIR_R2.json` is the active superseding lock and points to a fresh retry root. The scientific protocol remains byte-identical.

This task authorizes only a fresh, serial engineering-pilot launch after CPU validation and prelaunch gates. Any failed pre-runtime retry stays immutable and uses a new result root and superseding lock. No retry is allowed after real runtime cycles begin. The user will monitor the pilot after Codex confirms at least five actual cycle observations and leaves tmux running. A completed pilot would only authorize a new post-repair paired scientific validation **protocol freeze**, not scientific execution.

No collision/progress/noninferiority, parameter-selection, hard-real-time, or deployment claim follows from this pilot. Historical V3 remains `FAIL_V3_HARD_SAFETY_GATE`; the old result must not be rewritten.
