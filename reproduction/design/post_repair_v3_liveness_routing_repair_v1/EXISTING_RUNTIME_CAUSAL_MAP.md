# Existing runtime causal map (static source audit)

Source identity: frozen HEAD `63694588fba8bc6c6290cd3d0a337875d5040eaa`; no production call was made. Line references below are source lines at this HEAD.

1. `active_runtime_assurance_v2/active_cycle.py:392-730`: each `run_cycle` snapshots/validates a retained token, evaluates L1 once, then normally proposes P0, binds a fresh attempt, evaluates C0→L2→L3, routes every result through Supervisor, arbitrates, and delegates commit/trace to ActiveRunner. There is no persistent "terminal mode" shortcut: the next public cycle begins this sequence again.
2. `active_cycle.py:490-563`, `certification_execution_state_identity_repair_v1/repaired_components.py:189-244`, and `.../stack_factory.py:148-166`: L3 is reached only after matching L2 PASS. Its canonical witness builder calls the frozen BackupCertifier. L3 FAIL means that builder did not certify the candidate's finite braking-to-rest backup/terminal chain; L3 UNKNOWN covers mismatched L2 or backend exception. `certifier/backup_certifier.py:15-55` enumerates possible fail reasons: initial actuator/segment, zero actuator authority, backup actuator/segment, budget, or terminal certificate. The frozen scientific trace does **not** log the per-attempt L3 reason; no one of those reasons can be assigned to the bottom ten.
3. `active_cycle.py:535-562`, `supervisor.py:202-230`, `method_logic_closure_v2/STATE_TRANSITION_TABLE_V2.csv:27-32`: L3 status, candidate identity/role, deadline observation and retained-backup validity reach Supervisor routing. Existing L3-absent alternative search requires an already valid retained backup and OPEN deadline. With no valid backup it routes to arbitration/terminal evaluation. Current executable table has **44** rows (a post-PR #107 row was added); a future design must not pretend the runtime still has 43.
4. `backup_token_store.py:24-112`: no token is `BACKUP_NONE`/NOT_CREATED; a post-navigation-commit prepared bundle activates a token for k+1; `validate` requires ACTIVE lifecycle, exact snapshot/time/map/geometry/actuator/dynamics identity and current cursor action. Invalid/stale tokens FAIL. Successful backup commit advances cursor; end of tail becomes EXHAUSTED. `active_cycle.py:399-406,628-655` passes validated availability to Supervisor, without choosing backup priority. No stale token may be extended by reinterpretation.
5. `terminal_runtime.py:14-36`, `authority_registry.py:46,90`, `supervisor.py:441-447`: terminal is a current-state membership plus certificate, evaluated only on routed fallback. Goal-hold is disabled. Supervisor constructs the certified terminal **zero-acceleration** action; PlantCommit applies the same frozen dynamics. It is not a declaration that the state is unsafe, and it can be a legal hard-safe action without advancing goal progress.
6. `smoke/active_runtime_smoke_v2/run_active_runtime_smoke_v2.py:292-345`: terminal membership is braking-to-rest velocity membership; the production provider is `NativeExistingAlternativeProvider()` with no supplier. `alternative_provider.py:10-23` proves that this deployment's native inventory is empty by construction, while the architecture can accept lawful native candidates if a supplier is later provided. No synthetic candidate is currently authorized.
7. Frozen bottom-ten `cycle_observations.jsonl` (existing evidence, read only): all terminal steps had numerically equal pre/post state; for trial12 and witness57, all 500 steps were terminal with zero initial/final velocity and zero positional displacement. Cycle-indexed state **identities** differ even when numeric state is identical, so the fixed-point test must compare canonical numeric state, not snapshot identity. This supports a dynamic fixed-point interpretation, not a discrete state-machine lock.
8. `active_cycle.py:730-755`, `active_runner.py`, `plant_commit.py:25-54`, `trace_writer.py`: Supervisor alone selects, ActiveRunner preserves token/trace mechanics, PlantCommit alone executes. The canonical repair stack replaces L1/L2/L3/terminal/plant/token/trace wrappers with canonical arithmetic and identity ledger (`certification_execution_state_identity_repair_v1/stack_factory.py:116-210`); the design must reuse, not bypass, these wrappers.
9. `deadline_runtime.py:18-63`, `active_cycle.py:174-177,535-570,628-684`: DeadlineTracker observes OPEN/WARNING/EXPIRED; Supervisor interprets. WARNING/EXPIRED do not mean collision/certificate FAIL. Frozen cohort had EXPIRED=0 and only 5 WARNING observations; deadline is not supported as the primary liveness mechanism.

The causal chain supported by current evidence is: primary candidate often L1/C0/L2 PASS → L3 FAIL (exact per-attempt reason absent) → no valid backup / empty native alternative inventory → certified terminal zero acceleration → same numeric state → next cycle same opportunity pattern. It is a hypothesis about this frozen map/controller distribution, not a proof that a method repair will improve NI.
