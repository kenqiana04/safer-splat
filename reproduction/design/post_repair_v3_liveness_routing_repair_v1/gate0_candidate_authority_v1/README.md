# Bounded local recovery candidate authority: Gate 0

Design-only continuation of upstream `919c5518b07447d248293de1fe40023c0bfb8f9b`. `GATE0_PROTOCOL.json` was committed before static fixture outcomes. This directory authorizes **no** recovery runtime, GPU, trial, map query, QP, PlantCommit or scientific-verdict edit. See `IMPLEMENTATION_HANDOFF.md` for the next task only.

The V1 proposal is six axis-extremal actuator commands, not a complete safe-control search. All candidates require fresh binding, C0, L2 and canonical L3; only Supervisor may choose an action. The existing alternative source does not authorize this new source.
