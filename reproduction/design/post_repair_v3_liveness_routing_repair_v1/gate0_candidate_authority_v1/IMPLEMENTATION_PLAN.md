# Gate 0 execution plan

1. Verify exact upstream and create an isolated checkpoint branch/worktree.
2. Audit existing candidate/source identities, certification path and Supervisor authority.
3. Freeze source, F1 generator, exact order/dedup, trigger, priority, exhaustion and trace contracts.
4. Run CPU-only E01–E16 and R01–R24 static fixtures, validator and diff check.
5. Commit task-local protocol first, then completed design/results; attempt an existing remote push once without authentication repair.

No runtime implementation, map query, GPU or trial is authorized.
