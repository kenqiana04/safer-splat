# Next-task construction boundary (not executed here)

A. Expected changes: versioned recovery authority/provider/types in `reproduction/runtime/active_runtime_assurance_v2/`; additive candidate-local L3 reason/scope carrier in canonical L3 adapter; disjoint Supervisor-owned routing rows; additive Coordinator call site and trace schema; trial-owned exhaustion register. Scope must be separately reviewed against upstream protected-source manifest.
B. Forbidden: controller/QP, CBF math, dynamics, represented map, hard geometry/thresholds, L1/L2/L3 certificate mathematics, existing token lifecycle, PlantCommit semantics, scientific analyzer, Formal85 cohort and BYPASS behavior unless separately revalidated.
C. New ID `SOURCE_BOUNDED_LOCAL_RECOVERY_V1`; generator `AXIS_EXTREMA_F32_V1`; existing `ALTERNATIVE_NAVIGATION` execution role only after Supervisor selection.
D. New immutable typed source/scan/attempt/key/lifecycle/reason-scope records; no mutable global fallback policy.
E. Provider `enumerate(snapshot, authority, typed_entry_grant)` yields at most six exact ranked candidate records with fresh snapshot provenance; not the native-existing supplier and not a planner.
F. Supervisor route owns versioned recovery entry/continue/stop/fallback; exact-one, disjoint 43/44-rule extension with no first-match default; existing primary/backup/terminal precedence preserved.
G. Coordinator hook: after primary L3 typed local FAIL and validated no backup, route to current-state terminal prefetch, then bounded recovery if Supervisor grants; same-cycle L1 reused with fresh binding; each candidate C0→L2→L3; finally existing ActiveRunner commit path.
H. Append typed per-candidate normative evidence and scan-close event under existing trace/commit atomicity. No trace-derived decisions.
I. Trial-owned exhaustion register, keyed by exact numeric state and authority context; preserve across cycles only in trial; cursor prevents retries; incompatible authority invalidates routing use.
J. Unit tests: F1 vector bits/order/IDs, zero/asymmetric/unsupported bounds, primary/double-path dedup, source admission, L3 reason allowlist, terminal prefetch, deadline pause, cursor, authority change, selected bundle binding, causal horizon, no planner.
K. Exact-one R01–R24 plus full transition-table compatibility and negative missing/ambiguous routes; test all certification failures/unknowns.
L. BYPASS regression trigger: any shared `Supervisor`, `runtime_types`, trace/commit or source serialization semantic change touching BYPASS requires fresh BYPASS noninterference validation before ACTIVE conformance; do not assume historical evidence survives.
M. Before any GPU: separate immutable smoke protocol and CPU conformance/trace/commit/geometry checks. This Gate 0 authorizes neither implementation execution nor smoke.
