# Bounded post-repair V3 liveness/routing repair design V1

Design and static/CPU verification only. No runtime implementation, GPU work, new trials, map query, scientific reanalysis, parameter tuning, or change to either frozen verdict. `DESIGN_AUTHORITY.json` locks the input and hard boundaries; the other files compare mechanisms/options and specify a conditional, auditable future patch.
