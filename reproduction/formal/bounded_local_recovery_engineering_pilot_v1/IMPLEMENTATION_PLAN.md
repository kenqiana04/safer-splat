# Freeze Bounded Local Recovery Engineering Pilot V1

1. Verify Retry2 PASS artifacts, formal85 order authority, and the historical comparator read-only.
2. Freeze the 12-trial targeted engineering cohort from in-cohort anchors and deterministic evenly spaced controls.
3. Adapt the Retry2 runner/launcher/analyzer into an independent pilot harness and make the default monitor percentage-based.
4. Commit the protocol and harness before any future pilot outcome.
5. From the clean protocol commit, generate the execution lock and all authority/hash audits.
6. Run CPU-only syntax, freeze/prelaunch validation, monitor dry checks, and protected-diff checks.
7. Commit the lock/evidence, push without changing authentication, and verify remote/local HEAD equality.

No GPU, tmux, pilot result root, real trial, real PlantCommit, NI, Reference arm, or runtime-method mutation is authorized.
