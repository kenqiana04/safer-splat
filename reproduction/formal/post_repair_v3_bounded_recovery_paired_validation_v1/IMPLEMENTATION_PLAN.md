# Implementation Plan

1. Verify exact Git, Pilot, formal85, Reference, historical Active, map, and runtime-lineage authorities without execution.
2. Freeze the exact 85-trial cohort, hard-safety gate, progress non-inferiority contract, typed-boundary semantics, and integrity precedence.
3. Adapt the already validated bounded-recovery trial harness and formal85 analysis path into an independent task-local runner, launcher, percentage monitor, analyzer, and validator.
4. Commit the outcome-blind protocol before producing the execution lock.
5. Generate hash locks and CPU-only prelaunch validation, commit them separately, then push without changing Git authentication.

No GPU, tmux, result root, Reference rerun, Pilot rerun, runtime edit, or scientific result is authorized by this plan.
