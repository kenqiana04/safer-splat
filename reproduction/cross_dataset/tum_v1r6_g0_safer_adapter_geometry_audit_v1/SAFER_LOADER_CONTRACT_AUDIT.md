# SAFER Loader Contract Audit

`GSplatLoader` maps Nerfstudio means, wxyz rotations, exp-activated scales, sigmoid-activated opacities, and `compute_cov` covariances without filtering. Exact source paths and symbols are recorded in `loader/safer_loader_contract.json`.
