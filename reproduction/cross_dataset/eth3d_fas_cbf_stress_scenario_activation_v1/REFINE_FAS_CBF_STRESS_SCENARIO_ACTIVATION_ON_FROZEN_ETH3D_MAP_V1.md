# Frozen Protocol: ETH3D FAS-CBF Stress Activation V2

This task preserves PR #80 and its Case-B conclusion while repairing only the method-independent scenario generator. It reuses the same ETH3D Delivery Area learned map, independent reference mesh, M0-M4 code, controller bounds, `dt=0.05`, robot radius `0.10`, epsilon `0.01`, candidate budget `2000`, horizon `3`, max steps `200`, and seed `20260805`.

The protocol first recovers all V1 terminal and activation semantics. Candidate selection then uses a plant-free shadow pipeline and never reads rollout performance. The V2 registry contains 100 shared scenarios: 20 each for safe control, Start-Safe projection, feasibility dominance, sampled-data triggering, and predictive recovery. Pre-lock quotas are G0 H1/H3/H4 at most 2, G1 H1 at least 16 with 12 projection successes, G2 dominance at least 16, G3 DT triggers at least 16, and G4 recovery triggers at least 16 with 12 recoverable cases. The G3 composition must contain at least one endpoint-unsafe case, at least one endpoint-safe/segment-unsafe case, and margin-violation cases; unavoidable immediate-segment controls are capped at four of twenty rather than required to total four.

Three fresh registry generations must have identical raw bytes before 50 smoke runs. Only after smoke identity, trigger, terminal, leakage, and mutation gates pass may exactly 500 sequential formal runs execute. Formal failures remain terminal evidence. Statistical units are scenarios; paired comparisons and deterministic 10000-resample bootstrap intervals are preregistered. No map training/mutation, dataset switch, method tuning, reference-to-controller leakage, result-based scenario deletion, threshold relaxation, or scientific parameter change is authorized.
