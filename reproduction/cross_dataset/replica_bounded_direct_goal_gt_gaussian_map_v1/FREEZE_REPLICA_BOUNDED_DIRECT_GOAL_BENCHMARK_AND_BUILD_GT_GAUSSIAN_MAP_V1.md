# Freeze a Bounded Replica Direct-Goal Benchmark and Build a Certified GT-Derived Gaussian Safety Map V1

This task is the explicit successor to PR #63's valid pre-route block.  It does not recover missing historical bounds.  It freezes a new, versioned metric simulation contract and qualifies a deterministic full-mesh Gaussian safety geometry map without running a formal multi-step Replica navigation benchmark.

The sole successful downstream execution is `RUN_REPLICA_BOUNDED_DIRECT_GOAL_SAFER_FAS_CBF_BENCHMARK_V1`.  A learned external 3DGS remains a separate mandatory paper phase.
