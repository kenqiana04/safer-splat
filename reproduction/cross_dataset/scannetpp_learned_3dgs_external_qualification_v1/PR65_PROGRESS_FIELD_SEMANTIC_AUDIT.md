# PR #65 Progress Field Semantic Audit

`progress_m` is goal-distance reduction in metres: `||p0-goal|| - ||pT-goal||`. It is neither normalized progress nor path length. The frozen reported median 1.0137 therefore has unit metres.

Recommended paper label: `goal_distance_reduction_m` (retain the old raw value unchanged).
