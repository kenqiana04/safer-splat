# ETH3D Delivery Area 7zz Provisioning and Asset Contract Audit Implementation Plan

> **For agentic workers:** Execute this plan inline, in order, and fail closed
> at every frozen scientific gate. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Provision a trusted task-local 7zz, acquire exactly nine frozen ETH3D
Delivery Area archives, and finish the immutable asset/split/reference/UNKNOWN/
route contract audit without training or map generation.

**Architecture:** A task-local operational layer records every reversible
infrastructure action and supplies an absolute 7zz path. A deterministic Python
pipeline then performs sequential official downloads, archive security checks,
quarantine extraction, asset inventory, pose-only split construction, train-only
COLMAP filtering, reference/UNKNOWN/budget/route validation, and compact report
generation. Raw payload remains outside Git in physically separated roots.

**Tech Stack:** Python 3 standard library, NumPy/SciPy/Pillow/trimesh when already
available, official portable 7zz, curl through the managed proxy wrapper, COLMAP
text parsing, deterministic JSON/CSV/PNG evidence, Git/GitHub Draft PR.

---

### Task 1: Freeze lineage and operational policy

**Files:**
- Create: `pr78_blocker_preservation.json`
- Create: `operational_autonomy_policy_v1.json`
- Create: `operational_autonomy_actions.json`

- [x] Verify PR #78 and PR #77 identities from machine-readable artifacts.
- [x] Record the old blocker without editing any PR #78 file.
- [x] Record every infrastructure action with UTC time, scope, and effect.

### Task 2: Provision and qualify task-local 7zz

**Files:**
- Create: `provision_trusted_7z_runtime.py`
- Create: `validate_archive_runtime.py`
- Create: `task_local_archive_runtime_identity.json`
- Create: `archive_runtime_synthetic_validation.json`

- [x] Select T0/T1/T2/T3 in fixed order.
- [x] Freeze official URL, redirects, payload SHA, binary SHA, version, owner,
  mode, and provenance.
- [x] Run synthetic create, test, SLT list, extract-tree equality, Unicode path,
  and traversal pre-rejection checks.

### Task 3: Download and validate exactly nine archives

**Files:**
- Create: `download_frozen_eth3d_assets.py`
- Create: `validate_7z_archives.py`
- Create: `extract_eth3d_archives_safely.py`

- [x] HEAD-check the frozen official URL and Content-Length before each download.
- [x] Resume `.partial` files sequentially with at most three curl retries.
- [x] Verify size, SHA-256, CRC, SLT paths, link/device/collision/bomb safety.
- [x] Extract each safe archive to its own quarantine directory and freeze tree SHA.

### Task 4: Audit RGB/COLMAP and build the pose-only split

**Files:**
- Create: `inventory_eth3d_assets.py`
- Create: `audit_rig_capture_groups.py`
- Create: `audit_colmap_and_sparse_points.py`
- Create: `audit_dslr_cross_view.py`
- Create: `generate_pose_block_split_v1.py`
- Create: `build_train_only_colmap.py`

- [x] Verify rig/DSLR counts, decodability, grouping, calibration, pose, and
  image-only sparse-point provenance.
- [x] Generate primary and sensitivity splits without reference/map access.
- [x] Rebuild train-only COLMAP three times and require identical tree SHA.

### Task 5: Audit geometry, UNKNOWN, budget, routes, and evaluator

**Files:**
- Create: metric/reference/UNKNOWN/budget/route/evaluator scripts and records.

- [x] Prove metric frame and depth/reference semantics with two independent paths.
- [x] Qualify only official continuous geometry for route collision queries.
- [x] Freeze runtime UNKNOWN unchanged and run GT-only ideal support sensitivity.
- [x] Derive the stopping bound and route-specific physical reserve.
- [x] Run the corrected deterministic free-3D reference-only route gate. The
  first registry process failed the frozen 50% blocked-straight-line quota, so
  the remaining fresh-process reproductions and registry freeze stopped
  fail-closed as required.

### Task 6: Validate, report, and publish

**Files:**
- Create: required figures, `run_manifest.json`, `validation_result.json`,
  `downstream_handoff.json`, and the technical report.

- [x] Validate source data, calculations, figures, claims, and all zero boundaries.
- [x] Copy only compact evidence into the tracked root.
- [x] Stage the exact task root, commit, push through the managed wrapper, and
  create one Draft PR against the PR #78 branch.
