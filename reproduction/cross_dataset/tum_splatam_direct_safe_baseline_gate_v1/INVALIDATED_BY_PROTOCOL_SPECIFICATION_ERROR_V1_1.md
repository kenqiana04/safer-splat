# Invalidated pre-correction V1 calculation record

The old server root `tum_splatam_direct_safe_baseline_gate_v1` is retained
unchanged as historical evidence.  Its endpoint inventory, candidates, bins,
coarse/dense results, float64 certification, and any registry are
`INVALIDATED_BY_PROTOCOL_SPECIFICATION_ERROR` because the former protocol
mistook the PR #44 908-point synthetic static query set for a 908-center
transforms universe.

They are not V1.1 inputs, are not part of the V1.1 search budget, and cannot
be used to select a pair, infer a bin, freeze a registry, or support a final
statistic.  See `PROTOCOL_CORRECTION_CAMERA_UNIVERSE_V1_1.md`.
