# Resume Replica GT Executable-Safety Activated Benchmark V1

This task is the preregistered falsification benchmark for FAS-CBF Core V1 on the frozen Replica GT-FINE represented Gaussian map.

It freezes two separate cohorts before formal execution:

- `ACTIVATED`: selected only by frozen online stage predicates to test mechanism existence and causal attribution.
- `REPRESENTATIVE_HOLDOUT`: selected only by route/state provenance, fixed velocity strata, physical admissibility, and canonical hashing to estimate configuration-specific prevalence and utility.

The methods are the immutable PR86 matrix B0 through B3. PR84 certifier semantics, PR86 six-slot library, map/reference identities, dynamics, bounds, robot, margin, terminal set, braking policy, segment backend, candidate order, and all project decision thresholds remain unchanged.

Future reference outcomes and formal method runs are prohibited until both registries have three-process deterministic rebuild evidence and immutable lock markers. One-step evidence is the primary causal layer. Rollouts are bounded logical-time diagnostics, not real-time or deployment claims. Fail-closed means no certified action, not safe stop. Candidate exhaustion is limited to the frozen library and is not mathematical unrecoverability.
