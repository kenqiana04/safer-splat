# Bounded implementation plan

1. Verify exact upstream and old/new frozen authority identities; create the specified isolated worktree.
2. Commit the pre-outcome diagnostic protocol with fixed cohort, thresholds, mechanism labels, and claim limits.
3. CPU-only parse the existing 85 rows, trial summaries, and cycle traces; produce the specified task-local tables, descriptive statistics, and report. No runtime calls.
4. Validate exact cohort, frozen aggregate reproduction, input immutability, classifications, and output coverage; commit only task-local files and push if the existing remote works.
