# Post-repair V3 progress-NI failure diagnosis V1

CPU-only, read-only post-hoc diagnosis of the frozen 85-pair result. The frozen scientific decision remains `FAIL_POST_REPAIR_V3_PROGRESS_NONINFERIORITY_GATE`; this task never re-runs either arm or changes the gate. Diagnostic labels and associations are descriptive, not causal or efficacy verdicts.

Run the task-local analyzer against the authority paths in `DIAGNOSTIC_PROTOCOL.json`, then run the task-local validator. Raw evidence is read but not copied into this directory.
