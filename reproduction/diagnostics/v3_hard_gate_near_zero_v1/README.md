# V3 hard-gate near-zero diagnostic V1

Post-failure diagnostic for the four frozen Active V3 hard-safety violations in trials 22, 28, 57, and 59. The task asks whether the stored first-negative witnesses are float32 boundary phenomena or markers for deeper segment intrusion, and aligns each violating segment with the committed runtime action.

The protocol is frozen before any GPU query. All collection and analysis inputs are read-only. `0.015 q` remains the hard radius; `0.025 q` remains diagnostic-only with no runtime or scientific authority. This task cannot relabel the frozen scientific result.
