# L2/H1 Prospective Shadow Cohort V1 — Execution Plan

**Goal:** Collect exactly 100 preregistered Stonehenge `WRAPPER_ON` trials as an outcome-blind formal shadow cohort, freeze every raw artifact, and emit a collection lock before any scientific analysis.

**Upstream identity:** PR #100 head `84cf0734bafd52ddc7b100686fb0d1f509f1e356`; protocol raw-Git-blob SHA-256 `e8ea8f3fda15ab81664829ea6f71fcb37f772ad613c13f61007856c16117791a`.

## Task 1: Freeze task-owned code before data

- Create the five required collection scripts plus validators and synthetic fixtures.
- Verify raw Git blob identity for every PR #100 protocol input.
- Run syntax checks and outcome-blind synthetic failure tests.
- Confirm formal navigation count remains zero.

## Task 2: Freeze server execution identity

- Create an exact PR #100 checkout under the task-owned maintenance root.
- Audit Python/CUDA/GPU/map/controller/instrumentation identities.
- Verify physical GPU 1 is available and no task-owned process exists.
- Write `COLLECTION_EXECUTION_LOCK.json` containing all script and environment hashes.
- Revalidate the lock in a fresh process before the first intended step.

## Task 3: Collect the preregistered cohort serially

- Execute trial IDs 000 through 099 in manifest order, one fresh process at a time.
- Use only the frozen controller, frozen instrumentation, official map, fixed environment, and queue capacity.
- After each trial, run outcome-blind QC and raw-byte hashing.
- Permit at most one retry only for a verified zero-data pre-infrastructure failure.
- Stop the entire cohort on any post-data failure; never replace or rerun a scientific row.

## Task 4: Lock collection before analysis

- Require 100 terminal trials with per-trial completeness rates exactly 1.0 and all infrastructure counters zero.
- Build a deterministic raw artifact manifest and result-artifact commitment.
- Write and validate `FORMAL_COLLECTION_LOCK.json`.
- Do not calculate, print, store, or discuss L2 outcome distributions or scientific endpoints.

## Task 5: Traceable handoff

- Copy only compact outcome-blind evidence into Git.
- Run independent collection, systems, and reproducibility reviews.
- Commit only `reproduction/collection/l2_h1_prospective_shadow_cohort_v1/`.
- Push the task branch and create one Open Draft PR based on PR #100's branch.

## Stop conditions

- Upstream identity drift, protocol/raw-blob mismatch, protected-source mutation, GPU/environment/map/controller identity mismatch, execution-lock mismatch, or any post-data QC/runtime failure stops the cohort.
- Scientific analysis is forbidden until a valid 100-trial collection lock exists.
