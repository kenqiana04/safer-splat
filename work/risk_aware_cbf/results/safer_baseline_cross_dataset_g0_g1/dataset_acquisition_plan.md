# Acquisition Plan

The G0 inventory found no verified independent GSplat asset. G0.5A therefore fixes two official raw-data families before any new compatibility audit: TUM RGB-D `rgbd_dataset_freiburg1_room` and Replica Dataset v1. Raw assets, licenses, page snapshots, downloads, hashes, and any manual approval marker remain under `/disk1/zlab/cross_dataset_assets/` and outside Git.

TUM may be downloaded automatically from its official URL after the storage gate. Replica metadata may be cloned from its official repository, but Replica v1 download remains blocked until the user has personally created the required non-empty research-terms marker and its storage gate passes. Neither acquisition result establishes GSplat compatibility or SAFER baseline generalization.
