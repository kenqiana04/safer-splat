# V4-C Helper Dependency Closure

This directory records a provenance-grounded restoration of the two direct
Python helper dependencies required by the original V4-C recovery runner. It
contains static dependency inventories, import and symbol checks, one
deterministic interface-equivalence check, and an R1 Stage-0 reassessment.

No formal control was executed. No trajectory, benchmark trial, active
supervisor, recovery retuning, raw trace, image, model, or binary artifact was
produced. Barrier value `h` is not interpreted as meter clearance.

The source was `/disk1/zlab/projects/safer-splat` on
`master@57c55485e357343c3d166a9123ab9a9275c12067`. The source files were
untracked in that worktree, so provenance is supported by source metadata,
command-path evidence, and byte-identical July 2/3 local copies rather than a
claim that the helpers were Git-tracked at that commit.
