# V4-C Interface Restoration

## Status

This directory records reproducibility forensics and byte-identical restoration
only. No new scientific experiment, R1 shadow context audit, active supervisor,
recovery retuning, or formal control execution was run.

The four expected artifacts were recovered from provenance-confirmed 4090
sources and match corroborating local archives by SHA256. The restored V4-C
runner remains non-importable in the current Git branch because its two original
wrapper-level dependencies are absent and are not permitted restoration targets
in this task. No adapter or substitute recovery implementation was created.

Decision: `partially_restored_still_blocked`.
