# Risk-Aware CBF Metrics

This directory is reserved for compact, version-controlled metric files that are not tied to a single result subdirectory.

Use this directory for small aggregate CSV/JSON files needed by the paper-release manifest. Keep raw traces and bulky experiment dumps in ignored result locations or external storage.

